# art —— 美术资产（生成的，不是画的）

> ⚠ **先读 [资产定义](asset-list.md)（`docs/asset-list.md`）** —— 那是"这个游戏有哪些资产"的规格。
> 本文只管"怎么生成"。生成器是实现的一种，随时可以改/换；规格先定。


> **画风 = 6 条规则 + 一张色板。规则写在 `style.json`，代码照着规则画。**
> 改画风 → 改 `style.json`；加人 → 加 `characters.json` 一行。不用重画任何东西。

```bash
bash art/build.sh          # 一条命令：生成 → 校验 → 出联络表
# 验收在 wiki 里看：bash art/build.sh 会顺带重建 wiki
open wiki/out/art.html     # ★ 美术页 = 场景预览 + 旋钮（只看「摆一起协不协调」）
```

产物 `art/out/` 是**构建产物**（已 gitignore）—— 真源是下面三个文件。

---

## 〇、什么能删，什么不能删

```
art/style.json       ★ 不能删  画风真源（色板 / 6 条规则 / 建筑与人物参数）
art/characters.json  ★ 不能删  角色真源（8 人的可辨识性维度）
art/gen.mjs          ★ 不能删  生成器 —— 256 个资产全是它算出来的
art/verify.py        ★ 不能删  校验器（六条闸，见 §六）
art/bbox.py          ★ 不能删  包围盒计算（verify 的第 ⑥⑦ 条靠它）
art/build.sh         ★ 不能删  一条命令跑完全套
art/README.md        ★ 不能删  这份文档

art/out/             ✔ 可以删  构建产物（gitignore）。删了跑 build.sh 就回来。
art/__pycache__/     ✔ 可以删  Python 缓存
```

★ 但 **`art/out/` 其实也不用删** —— 它是【给游戏用的】产物（PixiJS 从这里加载）。
  wiki 有自己的一份拷贝（见下），所以两边互不影响。

## 〇之二、wiki 是自足的

`wiki/gen.mjs` 会把 `art/out/` 整个拷到 `wiki/out/a/`，页面里写的是 `a/xxx.svg`。

```
art/out/      ← 给游戏用（PixiJS 从这里加载）
wiki/out/a/   ← wiki 自己的一份（拷贝）
```

所以 **`wiki/out/` 可以整个 zip 出去发人看** —— 图不会断。
（拷完试过：整个目录搬到别处，断链 0 处。）

`wiki/verify.py` 第 ⑦ 条盯着这个：拿 `manifest.json` 逐个对，
少一个文件就报"先跑 bash art/build.sh"。

**为什么没有"直接交出 SVG 文件"这条路**：256 个文件、改一次色板就得手工改 256 处。
现在改 `style.json` 一行，重跑，全套跟着变。这是这套东西唯一的立足点。

---

## 一、这个方法擅长什么、不擅长什么

| | |
|---|---|
| ✅ **擅长** | 平铺贴图 · 模块化建筑 · 图标 · 规则化的小人 · 阴影与标记 · **改一次色板全局生效** |
| ❌ **不擅长** | 手绘质感 · 有性格的脸 · 氛围（光照/天气的美术感）· 需要"画味"的东西 |

所以这套东西的定位是：**把占位美术做到"能看"，并且做到"可整体替换"**。
它不取代美术 —— 它让美术来之前游戏就能跑，并且**给出确定的规格**（网格/锚点/命名/图层），
美术接手时不用猜。

**当它不够用时**（要质感、要表情、要氛围），按同一份清单交图替换即可：文件名、尺寸、
锚点、图层都已经定死（见 §五）。生成器只是"最省事的那一版实现"。

---

## 二、画风 = 这 6 条规则

写在 `style.json` 的 `rules` 里。**这是整套东西唯一的"审美判断"**：

```
① 平涂 + 1px 深色描边。不用渐变做体积（渐变只用在"高光偏一点"和"水"）。
② 俯视，但底部留 4~5px 的立面揭示 = 假的厚度。这是"看着立体"的全部秘密。
③ 落影单独一层（偏移 4,5 · 黑 10%），方便夜/雨复用、也方便关掉。
④ 一切对齐 32 网格。不许有半格的东西。
⑤ 单个资产最多 5 个颜色。多了就说明该拆图层。
⑥ 纹理 = 1px 重复图案，透明度 ≤22%。不用噪点、不用颗粒（缩小会糊成脏块）。
```

规则 ② 是原型的"城市感"最大的来源 —— 建筑底下那 5px 的墙，就是一个平涂矩形看起来
像房子的原因。

---

## 三、目录

```
style.json         ★ 画风真源：网格 / 色板 / 6 条规则 / 建筑与人物参数
characters.json    ★ 角色真源：8 人的"可辨识性维度"（身体/头发/上衣/配饰/肤色）
gen.mjs              生成器（零依赖 Node）
verify.py            校验器（生成的东西不能靠肉眼验收，见 §六）
build.sh             一条命令跑完：生成 → 校验 → 重建 wiki
out/                 构建产物（gitignore）
  manifest.json      给前端的清单（★ 带 cat/sub 分类 + 九宫格 + atmosphere）
  ground/ road/ props/ bld/ people/ items/ attach/ portraits/ fx/ atm/

验收在 wiki 里（不再有单独的联络表）：
  wiki/out/art.html   ★ 只做一件事：看它们摆在一起协不协调
                       4 个场景 × 旋钮（时间/天气/缩放/图层/格子）
  wiki/out/chars.html 人物：头像 + 三向走路循环 + 可辨识性表
```

---

## 四、怎么改

### 换画风
改 `style.json` 的 `palette`，重跑 → **256 个资产一起变**。
（比如把草地从 `#6d8a52` 改成秋色，整张地图立刻变季。）

### 关于锚点：头只有一个中心

画小人时，**头、头发、帽子、眼镜、眼睛必须挂在同一个锚点上**：

```js
const headX = cx + headShift;                        // 头的中心
const hairX = dir === "side" ? headX - B.headR * 0.32 : headX;   // 头发往后偏一点点
```

反复踩过的坑：头用 `cx+shift`、发盖用 `cx-headR*0.55`、帽子和长发又用回 `cx`。
**三个中心** —— 正面看没事，侧向就歪。现在 `verify.py` 第 ⑦ 条盯着这个。

偏移量别贪大：`0.32×头半径` 是"侧向头发往后掠"；
`0.55×头半径` 会让发盖整个滑到脑后，额头露一块白。

### 加一个人
`characters.json` 里加一行：

```json
{ "id":"n_chen", "name":"陈师傅", "body":"stocky", "skin":1,
  "hair":"white", "top":"#6b7a5a", "acc":"bag" }
```

重跑 → 自动生成他 3 向 × 5 帧 = 15 个文件 + 清单条目。
**不用碰 gen.mjs，也不用碰前端。**

新增后会自检：

```
$ node art/gen.mjs --check
角色可辨识性：9 人，两两 36 对
  ✓ 任意两人至少 3 维不同（身体/头发/上衣/配饰/肤色）
```

不满足会报出**是哪两个人在哪几个维度撞了**，比如：

```
✗ me(陈知远) vs n_lin(林静)：只差 2 维 [top,acc]
```

> 这条是契约的硬要求（`contracts.md`：可区分性 > 随机性）。
> 因为 Demo 的验收第①条就是「**我认得他们**」—— 玩家要一眼认出"那是王阿姨"。

### 加一个物品
`gen.mjs` 的 `ITEMS` 数组加一行（`{id, name, w, h, empty, world, icon, tags}`），
自动出三张：**世界形态（俯视）· 空态 · 图标（正视）**。

清单照 `config/items/*.json` 来 —— **物品是配置定义的，不是这里编的**。
现在有 19 件：

```
货·消耗品   苹果 · 梨 · 米 · 面包 · 牛奶 · 简餐 · 热菜
            饼干 · 汽水 · 咖啡                ← 会卖光
原料        简餐原料
家具        床 · 前台 · 工位 · 工业机器 · 马桶
容器        货架 · 冰柜                    ← 装货用，画法要留出"货摆在上面"的位置
工位        灶台
```

★ **画坐标必须过 `u()`**（`u = v => v * authorScale`）。
  `rect / circ / rrect / line / ell / dp` 这些助手内部都会调 `u()`，所以传**逻辑坐标**
  （16×16 的图标就写到 16 为止）。**直接写 `<path d="M 2.4 7.4 ...">` 会绕过缩放** ——
  画出来只有一半大、还缩在左上角。这个坑吃过一次，10 个物品图标全中招。

  要写裸 path 就用 **`dp("M ...", 'fill="...")`**，它会把 d 里的数字也过一遍 `u()`
  （圆弧 `a/A` 的 large-arc-flag / sweep-flag 是布尔，它认得，不会去缩放）。

  ★ 这类错肉眼很难发现（XML 合法、尺寸合规、颜色合法，所有检查都过），
  所以 `verify.py` 第 ⑥ 条会量**填充率**：固定格子类（图标/徽章/头像）
  的内容必须占满画布的 0.72 以上。忘了过 `u()` 会掉到 0.35〜0.5，当场报出来。

★ **物品需要两套"语言"，这是故意的**：

```
世界形态 = 俯视   地图是俯视的，货摆在地上要贴地
图标形态 = 正视   16px 的小图标，正视才认得出来 —— 俯视的苹果就是个圆
```

★ **空态 = "卖光了"的世界表达**（P6：能用形状就不用文字）。
只有会消耗的货才有空态，家具永远满。

### 加一栋楼
`gen.mjs` 里的 `BUILDINGS` 数组加一行（`{id, kind, g:[宽格,高格], v:变体}`），
自动生成三张：本体 / 落影 / 夜间窗光，外加门与招牌位的坐标写进 `manifest.json`。

---

## 五、产物契约（前端只认这个）

`out/manifest.json`：

```json
{ "grid": 32, "authorScale": 2, "logicalSize": true,
  "buildings": [ { "id":"shop_a",
                   "doors":[{"side":"south","offset":0.46}],
                   "sign":{"x":7,"y":6,"w":242,"h":13} } ],
  "assets": [ { "name":"n_wang_down_walk0", "file":"people/n_wang_down_walk0.svg",
                "w":18, "h":24, "anchor":"bottomcenter", "layer":"L4",
                "tags":["person","n_wang","down","walk","f0"] } ] }
```

| 字段 | 含义 |
|---|---|
| `w` / `h` | **逻辑尺寸**（不是文件里的像素尺寸） |
| `anchor` | `topleft` 地图件 · `bottomcenter` 立在地上的东西 · `center` 标记 |
| `layer` | L0 地面 · L1 路 · L2 装饰 · L3 建筑 · L4 实体 · L5 夜间 · L6 标注 |
| `authorScale: 2` | SVG 按 **2× 绘制、按 2× 声明**。Pixi 里 `sprite.scale = 1/2` |

**为什么 2×**：超过 2× 缩放时纹理才不会糊（相机上限 2.4×）。
SVG 是矢量的，浏览器里任意缩放都锐；但 Pixi 会把它栅格化成纹理，所以声明尺寸要给足。

**招牌上的字不进贴图** —— 招牌底留白，字由代码画。
因为编辑器能改建筑名，写进贴图就死了。

**锚点别改**：`bottomcenter` 的东西（树/人/车/长椅）你把锚点从脚底挪到中心，它们就全都
浮在半空。这是最容易犯又最难查的错。

---

## 六、为什么有 `verify.py`

生成的 256 个文件不能靠肉眼验收。它检查七件事：

```
① 清单里每个文件都在，且是合法 XML
② 声明尺寸 == 逻辑尺寸 × authorScale   ← 错了 Pixi 里就糊或者错位
③ 没有 NaN / undefined / null 混进坐标
④ 所有 url(#id) 都指得到对应 <pattern>  ← 引用不到 = 那块直接不画（静默失败）
⑤ 所有 fill / stroke 都是合法颜色
⑥ 固定格子类（图标/徽章/头像）的填充率 ≥ 0.72
⑦ 头发/帽子长在头上（发块中心 vs 头的圆心）
```

它抓过三个真问题：

```
② 落影画布故意比建筑大一圈（偏移余量），但清单里写成了建筑尺寸
   → 浏览器里只表现为"影子偏了一点"，肉眼几乎发现不了

⑥ 10 个物品图标忘了过 u()，只有一半大还缩在左上角
   → 所有其他检查全绿；正是「食物图标大的大小的小」那个问题

⑥ 饼干图标 r=5.2 偏小（填充率 0.65）—— 混在一堆 0.80 里的那个异类

⑦ 世界小人的头 / 发盖 / 帽子用了【三个不同的中心】：
  头 cx+0.8、发盖 cx-headR*0.55、帽子和长发又画回 cx
  → 侧向时帽子明显歪在脑袋旁边，前面还露出一块光头。
  8 个人全中招，但正面（down）看是好的 —— 所以一直没发现
```

---

## 七、已知能做到哪一步

```
✅ 256 个资产 · 241 KB · 人物 8 人 × 3 向 × 5 帧（左右靠镜像）+ 头像 8
✅ 物品 19 件 × (世界俯视 + 图标正视) + 4 张空态 + 存量刻度条
✅ 建筑附件 13（招牌底 6 九宫格 / 雨棚 3 / 烟囱 2 / 空调外机 2）
✅ 氛围：时间色调 5 档（参数，在 manifest.atmosphere）+ 雨 2
✅ 3 向（下/上/侧）+ 走 4 帧 + 站 1 帧 —— 2 帧一循环的身体浮动
✅ 建筑 12 栋 × 3 层（本体/落影/夜光）—— **12 种 config 建筑类型全覆盖**
✅ 改色板 → 全局生效
✅ 加人 → 一条命令
⚠ 走路只有腿摆 + 身体浮动，没有手臂摆动
⚠ 建筑只有 2 个变体（靠条纹方向/门位/颜色区分）—— 一条街会看出重复
⚠ 物品比现实略大 —— 故意的：要能点中（热区 ≥28 屏幕像素）
⚠ 还没有：招牌字（要）、室内地板贴图、雪/雾、autotile 过渡集

验收在 wiki：`wiki/out/art.html`（**只有场景预览 + 旋钮**）、
`wiki/out/chars.html`（人物）、物品/建筑各自的页面。

★ **美术页刻意只做一件事**：看它们摆在一起协不协调。
  单个资产的对错由 `verify.py` 机械地查（七条闸），代码能查的都查完了 ——
  剩下这一个问题只有眼睛能答，所以那一页就只留它，加几个旋钮能换角度看。
  资产目录 / 缺口清单在 `docs/asset-list.md`。

★ 场景会**自己报越界**（"⚠ N 个元素摆到了画布外"）。
  摆出画布的东西在代码里看不出来，肉眼也容易漏 —— 上一个版本就把一栋房子
  摆到了 640 宽的画布外（520+128=648），没人发现。
```
