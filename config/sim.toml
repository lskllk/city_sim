# 全局常量（原 Person/BasalMetabolism/ReviewClock 里魔法数字全部搬来）
# 信号统一 0..1 float。时间: tick=1 游戏分钟, 一天=1440 tick。

[time]
ticks_per_day = 1440

[metabolism]                 # 每 tick 基础 delta（负=下降）; 其余信号缺省 0.0
# 设计: 一天=1440 tick; 任一需求从满到 0 都 >2 天, 给 LLM 每日规划留足缓冲。
hunger = -0.00023148148148148148   # -1/4320（3 天）
thirst = -0.00027777777777777778   # -1/3600（2.5 天，最快）
energy = -0.00017361111111111112   # -1/5760（4 天）
fun    = -0.00017361111111111112   # -1/5760（4 天）

[bladder]
convert_per_tick = 0.02        # pending -> 膀胱 的每 tick 转化速率

[knowledge]                    # M5: 知识库
half_life_ticks = 2880        # 2 天(知识半衰期)

[utility]                      # 决策打分
power = 3.0                    # weight = need**power(幂>1 放大真缺口)
threshold = 0.0                # 效用最低分(置 0): 是否行动由 fallback_need 决定,
                               # eff 仍用于在多个候选中排序选优。
move_penalty = 0.6             # 异地(MoveTo)候选效用折扣(移动有耗时/精力成本)
fallback_need = 0.9            # 兜底触发下限: 仅 need>=此值才纳入候选
                               # 0.9 = 信号掉到 10% 以下(接近致命)才动手;
                               # 平时应由计划表系统驱动, 此处只当安全网。

[motion]                       # 跨地点移动(m5-rectify 13; M7 日程/距离前先入 config)
move_ticks = 30

[health]                       # 生命: 饥饿/口渴/精力任一为 0 时下降, 三者满足时回升
hp_decay = 0.00034722222222222222   # 1/2880: 关键信号归零后约 2 天掉光 hp
hp_regen = 0.00034722222222222222   # 三者满足时 hp 回升最大速率(×均值)
