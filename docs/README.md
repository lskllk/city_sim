# docs

## 活文档（3 份，其余都没有了）

| 文件 | 是什么 |
|---|---|
| **`checklist.md`** | **设计真源 + 现状/拍板记录**：定过什么 / 做到哪了 / 下一步做什么 |
| `naming.md` | 命名规则（`tests/test_naming.py` 强制） |
| `plan_timeline_viz.md` | 计划表时间线可视化（`plan_timeline.gd` 引用） |

> 2026-09-15 收敛 + 后续：早期按日设计稿（`20260910/`、`20260913/`、`20260914/` 各篇、
> `Vision.md`）与后来的 `design.md` **已全部删除** —— 都是与实现脱节的历史讨论。
> 机制/参数以 `config/*` 为准，现状以 `checklist.md` 为准。

## 约定

- 文件名小写 `snake_case` ASCII。
- 新增机制时的写法：**先改 `config/*` 数据 → 再改代码 → 最后在 `checklist.md` 补一段"是什么意思"**。
- 不写"代码落点速查"这类表 —— 代码会动，文档会烂。
