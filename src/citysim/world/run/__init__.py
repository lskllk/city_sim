"""world.run —— 世界的时钟: 每 tick / 到点就发生的事。

  engine    ★ 主循环 tick(): 13 个步骤的编排 —— 从这读起
  drive     谁该在这一刻决策(在途/排队的人不重算)
  travel    跨地点移动的推进
  gossip    传闻: 搭话/信任/气泡

不限于"每 tick": 招聘/发工资是每天一次, 补货是开门前一次 —— 它们都由
engine.tick() 按时刻转发进来, 所以都归这里。
"""
